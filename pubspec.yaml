name: name_list
description: 명함 관리 Flutter 앱

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  image_picker: ^1.0.4
  provider: ^6.1.1

  # (추후 OCR 기능용 예시. 지금은 주석 처리)
  # google_mlkit_text_recognition: ^0.12.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    # 추후 이미지 등 정적 리소스 쓸 때 여기에 경로 추가 가능
    # - assets/images/
